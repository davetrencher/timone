TunnelIJ

This plugin will allow you specify a port and a destination and will capture the TCP traffic.

For example:

Specify a src port of 8090.  Then specify the original destination ip/url and port
Send the request via localhost:8090 and the request will be forwarded to the specified destination.
You will be able to see the request/response in the split pane.